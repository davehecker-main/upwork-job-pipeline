{
  "name": "upwork-job-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Polls Upwork through its official MCP server, scores jobs with Claude against a written rubric, posts the survivors to Slack, and drafts a proposal for the ones you approve. Nothing auto-submits.",
  "scripts": {
    "test": "vitest run",
    "verify": "node scripts/verify-creds.mjs",
    "setup:slack": "node scripts/setup-slack.mjs",
    "poll": "node scripts/poll.mjs",
    "run": "node scripts/run-once.mjs",
    "approvals": "node scripts/check-approvals.mjs",
    "eval": "node scripts/run-eval.mjs"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
